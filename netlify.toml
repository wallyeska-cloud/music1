[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Netlify auto-detects Next.js and injects @netlify/plugin-nextjs.
# App Router route handlers (/api/*) run as serverless functions.
